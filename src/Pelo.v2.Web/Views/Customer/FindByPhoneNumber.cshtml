@*@model Pelo.v2.Web.Models.Customer.FindCustomerByPhoneViewModel
@{
    ViewData["Title"] = "Tìm kiếm khách hàng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ol class="breadcrumb">
    <li><a href="@Url.Action("Index","Customer")">Danh sách khách hàng</a></li>
    <li class="active">Thêm mới khách hàng</li>
</ol>
<section class="card">
    <div class="card-block">
        <form asp-controller="Customer" asp-action="FindByPhoneNumber" method="post" id="customer-form">
            @Html.ValidationSummary()
            @Html.HiddenFor(c=>c.NextAction)
            <div class="row">
                <div class="col-lg-12">
                    <fieldset class="form-group">
                        <nop-label asp-for="PhoneNumber" text-display="Số điện thoại" />
                        <input asp-for="PhoneNumber" class="form-control" type="text" placeholder="Số điện thoại" />
                        @Html.ValidationMessageFor(c => c.PhoneNumber)
                    </fieldset>
                </div>
                <div class="col-lg-12">
                    <fieldset class="form-group">
                        <button type="submit" class="btn btn-primary">Tìm kiếm</button>
                        <a class="btn btn-default" href="@Url.Action("Index", Model.NextAction)">Quay lại</a>
                    </fieldset>
                </div>
            </div>
        </form>
    </div>
</section>
@section js{
    <script>
        $(document).ready(function() {
            $('.opened').removeClass('opened');
            $('.row-active').removeClass('row-active');
            $('#liCustomers').addClass('opened');
            $('#liCustomer').addClass('row-active');
        });
    </script>
}*@

