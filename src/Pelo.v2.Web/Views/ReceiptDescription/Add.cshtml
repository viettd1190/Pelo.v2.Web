@model Pelo.v2.Web.Models.ReceiptDescription.ReceiptDescriptionInsert

@{
    ViewBag.Title = "Thêm mới dịch vụ";
    Layout = "_Layout";
}

<ol class="breadcrumb">
    <li><a href="@Url.Action("Index","ReceiptDescription")">Danh sách dịch vụ</a></li>
    <li class="active">Thêm mới dịch vụ</li>
</ol>

<section class="card">
    <div class="card-block">
        <form asp-controller="ReceiptDescription" asp-action="Add" method="post" id="receiptdescription-form">
            @Html.ValidationSummary()
            <div class="row">
                <div class="col-lg-12">
                    <fieldset class="form-group">
                        <label class="form-label semibold" for="Name">Tên</label>
                        <input asp-for="Name" class="form-control" type="text" placeholder="Tên"/>
                        @Html.ValidationMessageFor(c => c.Name)
                    </fieldset>
                </div>
                <div class="col-lg-12">
                    <fieldset class="form-group">
                        <button type="submit" class="btn btn-primary">Thêm mới</button>
                        <a class="btn btn-default" href="@Url.Action("Index", "ReceiptDescription")">Hủy</a>
                    </fieldset>
                </div>
            </div>
        </form>
    </div>
</section>

@section js{ 
    <script>
        $(document).ready(function () {
            $('.opened').removeClass('opened');
            $('.row-active').removeClass('row-active');
            $('#liReceipt').addClass('opened');
            $('#liReceiptDescription').addClass('row-active');
        })
    </script>
}
